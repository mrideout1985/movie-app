import Head from 'next/head'
import styles from '../styles/Home.module.scss'
import api from "./api/index"
import Meta from '../components/meta/meta'
import Card from '../components/card/card'


export default function Home(props) {

  console.log(props)
  return (
    <div className={styles.container}>
      <Meta title={"HOME"}>
        <meta name="description" content="Generated by create next app" />
      </Meta>
      <h2>Popular Movies</h2>
      <div className={styles.popular}>
        {props.HeadpopMovieData ?? props.popMovieData.results.map((movie, i) => {
          return (
            <Card key={i} id={movie.id} poster_path={movie.poster_path} title={movie.title} release_date={movie.release_date} vote_average={movie.vote_average} />
          )
        })}
      </div>

      <div className={styles.nowplaying}>

      </div>


    </div>
  )
}

export const getStaticProps = async () => {
  const popularMovieData = await api.popularMovies()


  return {
    props: {
      popMovieData: popularMovieData,
    }
  }
};